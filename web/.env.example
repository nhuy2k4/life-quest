# Local dev can use the Vite proxy with /api/v1.
VITE_API_URL=/api/v1

# Production on Vercel should point to the Render backend API.
# Example:
# VITE_API_URL=https://your-backend.onrender.com/api/v1
